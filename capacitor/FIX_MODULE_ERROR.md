# Исправление ошибки "Module not specified"

## Проблема
Android Studio не видит модуль `android.app` в списке.

## Решение 1: Открыть правильную папку проекта

**Важно:** Android Studio должен открывать папку `android`, а не `capacitor`!

### Правильно:
```
File → Open → выберите: /opt/eblusha-plus/capacitor/android
```

### Неправильно:
```
File → Open → /opt/eblusha-plus/capacitor  ❌
```

### Как проверить:
1. Посмотрите в левом верхнем углу Android Studio
2. Должно быть: `android` (не `capacitor`)
3. В структуре проекта должны быть папки: `app`, `capacitor-cordova-android-plugins`

## Решение 2: Синхронизация Gradle

Если папка правильная, но модуль не виден:

1. **Синхронизируйте проект:**
   - `File → Sync Project with Gradle Files`
   - Или нажмите иконку 🐘 (Gradle Sync) в панели инструментов
   - Дождитесь завершения синхронизации

2. **Проверьте структуру:**
   - В левой панели должен быть модуль `app`
   - Если его нет - проект открыт неправильно

## Решение 3: Переоткрыть проект

1. `File → Close Project`
2. `File → Open`
3. Выберите: `/opt/eblusha-plus/capacitor/android`
4. Дождитесь индексации и синхронизации Gradle

## Решение 4: Если модуль все еще не виден

### Проверьте файл settings.gradle:

Должен содержать:
```gradle
include ':app'
include ':capacitor-cordova-android-plugins'
```

### Очистите и пересоберите:

```bash
cd /opt/eblusha-plus/capacitor/android
./gradlew clean
```

Затем в Android Studio:
- `File → Sync Project with Gradle Files`
- `Build → Rebuild Project`

## Решение 5: Создать конфигурацию вручную

Если модуль все еще не появляется, можно указать путь вручную:

1. В конфигурации запуска
2. Поле **Module:** оставьте пустым или введите: `app`
3. В поле **Package:** укажите: `org.eblusha.plus`
4. В поле **Activity:** укажите: `org.eblusha.plus.MainActivity`

Но лучше исправить проблему с модулем (решения 1-4).

## Проверка правильности открытия проекта

После открытия `/opt/eblusha-plus/capacitor/android`:

✅ **Правильно, если видите:**
- Папка `app/` в структуре проекта
- Папка `capacitor-cordova-android-plugins/`
- Файл `build.gradle` в корне
- Файл `settings.gradle`

❌ **Неправильно, если видите:**
- Только `capacitor.config.ts`
- `package.json`
- `node_modules/`
- Нет папки `app/`

## Быстрая проверка:

```bash
# Проверьте, что папка android существует и содержит app
ls -la /opt/eblusha-plus/capacitor/android/app/
```

Должны быть файлы: `build.gradle`, `src/`, `AndroidManifest.xml`

