# Как запустить приложение

## Проблема: "как будто ничего не происходит"

Если в Android Studio видно `[OFFLINE]` в Logcat, значит нет подключенного устройства или эмулятора.

## Решение 1: Запустить эмулятор

### В Android Studio:
1. Нажмите на иконку **Device Manager** (📱) в правом верхнем углу
   - Или: `Tools → Device Manager`
2. Если эмулятор уже создан:
   - Нажмите ▶️ (Play) рядом с эмулятором
3. Если эмулятора нет:
   - Нажмите **Create Device**
   - Выберите устройство (например, Pixel 5)
   - Выберите системный образ (рекомендуется API 33-34)
   - Нажмите **Finish** и дождитесь загрузки
   - Затем нажмите ▶️ для запуска

### Через командную строку:
```bash
# Список доступных эмуляторов
emulator -list-avds

# Запустить эмулятор (замените на имя вашего AVD)
emulator -avd Pixel_5_API_33
```

## Решение 2: Подключить физическое устройство

1. **Включите режим разработчика:**
   - Настройки → О телефоне → 7 раз нажмите на "Номер сборки"
   
2. **Включите отладку по USB:**
   - Настройки → Для разработчиков → Отладка по USB

3. **Подключите устройство через USB**

4. **Разрешите отладку:**
   - На телефоне появится запрос "Разрешить отладку по USB?" → **Разрешить**

5. **Проверьте подключение:**
   ```bash
   adb devices
   ```
   Должно показать ваше устройство

## После подключения устройства/эмулятора:

1. В Android Studio выберите устройство в выпадающем списке (вверху, рядом с кнопкой Run)
2. Нажмите **Run** (▶️) или `Shift+F10`
3. Или через терминал:
   ```bash
   cd /opt/eblusha-plus/capacitor/android
   ./gradlew installDebug
   ```

## Проверка подключения:

```bash
# Проверить подключенные устройства
adb devices

# Если устройство не видно, попробуйте:
adb kill-server
adb start-server
adb devices
```

## Если эмулятор не запускается:

1. Проверьте, что установлен HAXM (Intel) или Hypervisor (AMD)
2. Включите виртуализацию в BIOS
3. Убедитесь, что достаточно RAM (минимум 4GB для эмулятора)

## Быстрая проверка:

```bash
# Проверить устройства
adb devices

# Если пусто - нет подключенных устройств
# Если есть устройство - можно запускать приложение
```

