uploads/
tmp/

.env
.env.*
*.env
*.env.*
!.env.example

# SSL certs (self-signed for stoned.local) — ключи не коммитить
/deploy/coturn/turnserver.conf
/deploy/coturn/certs/
/deploy/certs/

# editor/tooling backup files
*.bak

# build artifacts
/dist
/frontend/dist
/node_modules
/frontend/node_modules
/frontend/public/updates

# Local DB dumps / huge test payloads (GitHub file limit 100 MB)
/backups/
frontend/public/test200.bin

# accidental pasted shell artifact at repo root
/ing-box*

# Диагностические логи VPN-клиента, загруженные через PUT (runtime, не в git)
vpn-logs/
