-- Избранное убрано из продукта: концепт не прижился, отдельная «звезда» рядом с
-- реакциями только дублировала их. На момент удаления в таблице ноль строк —
-- проверено запросом перед миграцией, терять нечего.
DROP TABLE IF EXISTS "CloudFavorite";
